# Python
__pycache__/
*.pyc

# Distribution archives (rebuildable — never pushed to GitHub)
*.zip

# Trainer-only credentials and local environment files
.env
.env.*
!.env.example
courseware/archive/
.claude/skills/non-wsq-courseware-build/build/data_domain* 2.py

# Playwright
.playwright-mcp/

# OS
.DS_Store
Thumbs.db

# Screenshots containing real accounts or personal data — never published
*-original.png

# Source material the project was derived from — not ours to republish.
**/reference/
# ...but skill builder scripts are a reference implementation the repo needs.
!.claude/skills/**/reference/
!.claude/skills/**/reference/**
